pipeline {
    agent any
    stages {
        stage('Test'){
            steps{
                sh 'bash tests/tests.sh'
            }
        }
        // stage('Build'){

        // }
        // stage('Push'){

        // }
        // stage('Configuration Management(Ansible)'){

        // }
        // stage('Deploy'){
            
        // }
    }
}