pipeline {
    agent any
    stages {
        stage('Test'){
            steps{
                sh 'bash tests/test.sh'
            }
        }
        // stage('Build'){
            
        // }
        // stage('Push'){

        // }
        // stage('Configuration Management(Ansible)'){

        // }
        // stage('Deploy'){
            
        // }
    }
}