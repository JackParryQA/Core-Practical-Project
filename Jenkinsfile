pipeline {
    agent any
    stages {
        stage('Test'){
            sh 'bash tests/test.sh'
        }
        // stage('Build'){

        // }
        // stage('Push'){

        // }
        // stage('Configuration Management(Ansible)'){

        // }
        // stage('Deploy'){
            
        // }
    }
}